version: '3.8'

services:

  ifn:
    container_name: ifn
    image: node:22.3.0-alpine3.20
    volumes:
      - ./app:/app
      - node-cache:/usr/local/share/.cache
    working_dir: /app
    ports:
      - "3333:3000"
    environment:
      - WDS_SOCKET_PORT=0

volumes:
  node-cache:
